.Rproj.user
.Rhistory
.RData
.Ruserdata
.DS_Store

/.quarto/
**/*.quarto_ipynb

# Student data — lives in OneDrive "3. Teaching/_student-data/apm-lab", never in git
grading/
*.csv
ExportClassRoll*

# Symlinks into OneDrive (student data, heavy materials) - never in git
_private
_materials

# Secrets - never in git (all these repos are public)
.env
.env.local
_secrets
